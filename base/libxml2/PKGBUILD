#!/bin/bash

pkgname=libxml2
pkgver=2.9.4
archive=$pkgname-$pkgver.tar.gz
sum=958ae70baf186263a4bd801a81dd5d682aedd1db

dstdir=$mkToolchainBase

build() {
  download ftp://xmlsoft.org/libxml2/$archive
  check $archive $sum
  extract $archive

  pushd $pkgname-$pkgver
    message "Configuring $pkgname"
    ./configure --build=$TARGET --prefix="$dstdir" --disable-static \
                --with-thread --with-icu --with-history --without-python
    message "Compiling $pkgname"
    make
  popd
}

package() {
  make -C $pkgname-$pkgver install
  # TODO Python binding is disabled, it requires iconv and the configuration
  # script point to the system headers directly.
  # pushd $pkgname-$pkgver/python
  #   python setup.py install --prefix="$dstdir"
  # popd
}
