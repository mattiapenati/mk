#!/bin/bash

pkgname=pax-utils
pkgver=1.0.5
archive=$pkgname-$pkgver.tar.xz
sum=e3de243098c510570541f90012f753e8bd2a6886

dstdir=$mkToolchainBase

build() {
  download http://dev.gentoo.org/~vapier/dist/$archive
  check $archive $sum
  extract $archive

  pushd $pkgname-$pkgver
    message "Configuring $pkgname"
    ./configure --build=$TARGET --prefix="$dstdir" --disable-silent-rules
    message "Compiling $pkgname"
    make
  popd
}

package() {
  make -C $pkgname-$pkgver install
  strip "$dstdir"/bin/{dumpelf,pspax,scanelf,scanmacho}
  compress_doc "$dstdir"/share/man/man1/{dumpelf,pspax,scanelf,scanmacho}.1
}
