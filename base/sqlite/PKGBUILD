#!/bin/bash

pkgname=sqlite
pkgver=3170000
archive=$pkgname-autoconf-$pkgver.tar.gz
sum=7bcff1c158ed9e2c0e159c1b4b6c36d4d65dff8c

dstdir=$mkToolchainBase

build() {
  download https://www.sqlite.org/2017/$archive
  check $archive $sum
  extract $archive

  pushd $pkgname-autoconf-$pkgver
    message "Configuring $pkgname"
    ./configure --build=$TARGET --prefix="$dstdir" --disable-static
    message "Compiling $pkgname"
    make
  popd
}

package() {
  make -C $pkgname-autoconf-$pkgver install
}
