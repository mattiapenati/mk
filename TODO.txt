 * updating the bootstrap, using busybox-1.24.1 and lua-5.3.2
 * fix pax-utils, lddtree does not work properly with shared libraries, it does
   not detect the proper dynamic loader

 * blacs package
 * scalapack package
 * scotch package

 * suitesparse package (openblas + metis)
 * mumps package (openblas + blacs + scalapack + scotch)
 * superlu package (openblas + metis)
 * pardiso package
 * pastix package

 * boost package
 * qhull package

 * scipy stack package
    * numpy (openblas + suitesparse + fftw)
    * scipy (numpy)
    * ipython
    * pandas
    * sympy
    * nose
 * hdf5 package (scipy)
 * netcdf package (hdf5 + scipy)

 * eigen package
 * lis package
 * petsc package
 * trilinos package

 * getfem package
 * dealii package
 * fenics package

 * octave package
 * R package
