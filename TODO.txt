 * updating the bootstrap, using busybox-1.24.1 and lua-5.3.2
 * fix pax-utils, lddtree does not work properly with shared libraries, it does
   not detect the proper dynamic loader

 * suitesparse package (openblas + metis) (missing metis)
 * mumps package (openblas + blacs + scalapack + scotch)
 * superlu package (openblas + metis)
 * pastix package (openblas + metis + scotch)

 * boost package
 * glpk package
 * qrupdate package

 * scipy stack package
    * numpy (openblas + suitesparse + fftw)
    * scipy (numpy)
    * pandas
    * sympy
    * nose
    * cython
    * ipython
    * jupyter

 * hdf5 package (scipy)
 * netcdf package (hdf5 + scipy)

 * eigen package
 * lis package
 * petsc package
 * trilinos package

 * getfem package
 * dealii package
 * fenics package

 * octave package
 * R package
